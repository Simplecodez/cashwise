export enum AccountType {
  SAVINGS = 'savings',
  CURRENT = 'current'
}

export enum AccountStatus {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
  LOCKED = 'locked'
}

export enum AccountJobType {
  CREATION = 'creation'
}
